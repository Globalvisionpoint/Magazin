{% liquid
  assign theme_rtl = false
  if settings.enable_rtl and settings.langauges_rtl == blank
    assign theme_rtl = true
  endif

  if settings.enable_rtl and settings.langauges_rtl != blank
    assign rtl_languages = settings.langauges_rtl | split: ','
    for language in rtl_languages
      if language == request.locale.iso_code
        assign theme_rtl = true
      endif
    endfor
  endif
%}

<style>
  .product__sticky {
    position: fixed;
    left: 0;
    right: 0;
    bottom: calc(var(--mobile-navigation-bar-height, 0px) + env(safe-area-inset-bottom, 0px));
    z-index: 98;
    padding: 0.75rem 0.9rem;
    background: rgba(255, 255, 255, 0.98);
    border-top: 1px solid rgba(18, 18, 18, 0.08);
    box-shadow: 0 -8px 24px rgba(0, 0, 0, 0.14);
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transition: opacity 0.25s ease, transform 0.25s ease, visibility 0.25s ease;
    transform: translateY(100%);
  }

  .product__sticky.sticky {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
    transform: translateY(0);
  }

  .product-form__buttons.sticky__form_button {
    width: 100%;
    margin: 0 !important;
    display: flex;
    align-items: center;
    justify-content: flex-start;
    gap: 0;
  }

  .product__sticky_head {
    display: none;
  }

  .product__sticky_add__form {
    width: 100%;
    gap: 0.5rem;
  }

  .product__sticky_add__form .product-form,
  .product__sticky_add__form .form {
    width: 100%;
  }

  .product__sticky_price {
    flex: 0 0 auto;
    min-width: max-content;
    margin-left: 0.7rem;
    margin-right: 0.7rem;
    font-size: 1.7rem;
    font-weight: 900;
    line-height: 1;
    color: #c91f39;
    white-space: nowrap;
  }

  sticky-variant-select.product__sticky_variant {
    display: none;
    width: 100%;
    margin-bottom: 0;
  }

  sticky-variant-select .select {
    position: relative;
  }

  sticky-variant-select .select_box {
    width: 100%;
    min-width: 0;
    height: 4.4rem;
    padding: 0 3.4rem 0 1.2rem;
    border-radius: 1.2rem;
    border: 1px solid rgba(18, 18, 18, 0.12);
    background: #ffffff;
  }

  .product__sticky_add__form quantity-input.quantity {
    flex: 0 0 7.8rem;
    min-width: 7.8rem;
    height: 4.8rem;
    margin-left: 0.8rem;
    margin-right: 0;
    border: 1px solid rgba(18, 18, 18, 0.12);
    border-radius: 1.3rem;
    background: #ffffff;
    box-shadow: none;
    overflow: hidden;
  }

  .product__sticky_add__form .quantity__button {
    width: 3rem;
    color: #222222;
    background: transparent;
    transition: background 0.2s ease, color 0.2s ease;
  }

  .product__sticky_add__form .quantity__button:hover {
    background: rgba(217, 45, 47, 0.08);
    color: #c91f39;
  }

  .product__sticky_add__form .quantity__input {
    font-size: 1.5rem;
    font-weight: 600;
    color: #222222;
  }

  .product__sticky_add__form .product-form {
    flex: 1 1 auto;
    min-width: 0;
  }

  .product__sticky_add__form .product__add__cart__button,
  .product__sticky_add__form .product__add__cart__button.mr-20 {
    flex: 0 0 clamp(12rem, 40vw, 14.8rem);
    max-width: clamp(12rem, 40vw, 14.8rem);
    width: clamp(12rem, 40vw, 14.8rem);
    margin-right: 10px;
    margin-left: auto;
    min-width: 0;
  }

  .product__sticky_add__form .product__add__cart__button .button {
    width: 100%;
    min-height: 4.8rem;
    padding: 0 !important;
    border: 0 !important;
    border-radius: 0.35rem;
    background: #f3da63 !important;
    color: #202020 !important;
    display: inline-flex;
    align-items: stretch;
    justify-content: flex-start;
    overflow: hidden;
    font-family: var(--font-heading-family);
    box-shadow: none;
  }

  .product__sticky_add__form .product__add__cart__button .action__btn--svg {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    flex: 0 0 5.6rem;
    align-self: stretch;
    background: #bf1835;
    color: #ffd84d;
  }

  .product__sticky_add__form .product__add__cart__button .action__btn--svg svg {
    display: block;
    width: calc(2.45rem - 1px);
    height: calc(2.45rem - 1px);
    filter: drop-shadow(0 1px 0 rgba(120, 85, 0, 0.55));
    transform: translateX(-2px);
  }

  .product__sticky_add__form .product__add__cart__button .cart--button-text {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    flex: 1 1 auto;
    padding: 0 0.8rem;
    font-size: 1.16rem;
    font-weight: 700;
    letter-spacing: 0;
    line-height: 1;
    text-align: center;
    text-transform: uppercase;
    white-space: nowrap !important;
    color: #1f1f1f;
    background: #f3da63;
    transform: translateX(-6px);
  }

  .product__sticky_add__form .product__add__cart__button .button[disabled] {
    justify-content: center;
    background: #000000 !important;
    color: #ffffff !important;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.28);
  }

  .product__sticky_add__form .product__add__cart__button .button[disabled] .cart--button-text {
    justify-content: center;
    width: 100%;
    flex: 1 1 100%;
    text-align: center;
    padding: 0 1rem;
    margin: 0;
    background: #000000;
    color: #ffffff;
  }

  .product__sticky_add__form .shopify-payment-button {
    display: none !important;
  }

  @media only screen and (min-width: 992px) {
    .product__sticky {
      display: none !important;
    }

    body.sticky__cart {
      padding-bottom: 0 !important;
    }
  }

  @media only screen and (max-width: 991px) {
    body.sticky__cart {
      padding-bottom: calc(8.6rem + var(--mobile-navigation-bar-height, 0px));
    }

    .product__sticky_add__form .product__add__cart__button,
    .product__sticky_add__form .product__add__cart__button.mr-20 {
      flex: 0 0 calc(clamp(12rem, 40vw, 14.8rem) + 20px);
      max-width: calc(clamp(12rem, 40vw, 14.8rem) + 20px);
      width: calc(clamp(12rem, 40vw, 14.8rem) + 20px);
      margin-right: 10px;
      margin-left: auto;
    }
  }

  @media only screen and (max-width: 479px) {
    .product-form__buttons.sticky__form_button {
      gap: 0;
    }

    .product__sticky_add__form {
      gap: 0.5rem;
    }

    .product__sticky_add__form .product__add__cart__button,
    .product__sticky_add__form .product__add__cart__button.mr-20 {
      flex: 0 0 calc(clamp(11.8rem, 42vw, 14.5rem) + 20px);
      max-width: calc(clamp(11.8rem, 42vw, 14.5rem) + 20px);
      width: calc(clamp(11.8rem, 42vw, 14.5rem) + 20px);
      margin-right: 10px;
      margin-left: auto;
    }

    .product__sticky_add__form .product__add__cart__button .button {
      min-height: 4.8rem;
    }

    .product__sticky_add__form .product__add__cart__button .action__btn--svg {
      flex-basis: 4.6rem;
    }

    .product__sticky_add__form .product__add__cart__button .cart--button-text {
      font-size: 1.14rem;
      padding: 0 0.7rem;
      transform: translateX(-6px);
    }
  }

  {% if theme_rtl %}
    .product-form__buttons.sticky__form_button {
      flex-direction: row-reverse;
    }

    .product__sticky_add__form .product__add__cart__button,
    .product__sticky_add__form .product__add__cart__button.mr-20 {
      margin-right: auto;
      margin-left: 0.8rem;
    }

    .product__sticky_add__form quantity-input.quantity {
      margin-left: 0;
      margin-right: 0.8rem;
    }
  {% endif %}
</style>
<script src="{{ 'product-sticky-cart.js' | asset_url }}" defer="defer"></script>
{%- assign product_form_id = 'product-sticky-form-' | append: section.id -%}
<div class="product__sticky d-flex align-items-center justify-content-center {% if product.has_only_default_variant %}sticky__box_width{% endif %}">
  <div class="product__sticky_head d-flex align-items-center {% unless product.has_only_default_variant %}d-md-none{% endunless %}">
    <div class="sticky_pro_img">
      <img
        src="{{ product.featured_image | image_url: width: 120 }}"
        alt="{{ product.featured_image.alt | escape }}"
        width="60"
        height="{{ 60 | divided_by: product.featured_image.aspect_ratio | ceil }}"
        loading="lazy"
      >
    </div>
    <div class="sticky__proudct_title">
      {{ product.title }}
      {%- if product.has_only_default_variant -%}
        <p>{{ product.price | money }}</p>
      {%- endif -%}
    </div>
  </div>

  {%- unless product.has_only_default_variant -%}
    <sticky-variant-select class="product__sticky_variant" data-section="{{ section.id }}">
      <div class="select">
        <select
          id="sticky__variant"
          class="select_box"
          name="options[]"
          form="product-sticky-form-{{ section.id }}"
        >
          {%- for variant in product.variants -%}
            <option
              value="{{ variant.id }}"
              {% if variant.available == false %}
                disabled="disabled"
              {% endif %}
            >
              {{ variant.title }} - {{ variant.price | money }}
            </option>
          {%- endfor -%}
        </select>
        {% render 'icon-caret' %}
      </div>
    </sticky-variant-select>
  {%- endunless -%}
  <div class="product__sticky_add__form d-flex align-items-center">
    <quantity-input class="quantity mr-20">
      <button class="quantity__button no-js-hidden" name="minus" type="button">
        <span class="visually-hidden">
          {{- 'products.product.quantity.decrease' | t: product: product.title | escape -}}
        </span>
        {% render 'icon-minus' %}
      </button>
      <input
        class="quantity__input"
        type="number"
        name="quantity"
        id="Sticky-Quantity-{{ section.id }}"
        min="1"
        value="1"
        form="product-sticky-form-{{ section.id }}"
      >
      <button class="quantity__button no-js-hidden" name="plus" type="button">
        <span class="visually-hidden">
          {{- 'products.product.quantity.increase' | t: product: product.title | escape -}}
        </span>
        {% render 'icon-plus' %}
      </button>
    </quantity-input>
    <div class="product__sticky_price">
      <span class="product__sticky_price-value">{{ current_variant.price | money }}</span>
    </div>
    <product-form class="product-form">
      {%- form 'product',
        product,
        id: product_form_id,
        class: 'form',
        novalidate: 'novalidate',
        data-type: 'add-to-cart-form'
      -%}
        <input id="sticky__selected_variant_id" type="hidden" name="id" value="{{ current_variant.id }}">
        <div class="product-form__buttons sticky__form_button d-flex">
          <div class="product__add__cart__button mr-20">
            <button
              type="submit"
              name="add"
              class="button button--primary"
              {% if current_variant.available == false and product.has_only_default_variant %}
                disabled
              {% endif %}
            >
              {%- if current_variant.available == false and product.has_only_default_variant -%}
                <span class="cart--button-text">{{ 'products.product.sold_out' | t }}</span>
              {%- else -%}
                <span class="action__btn--svg">{% render 'icon-cart', icon: 'cart' %}</span>
                <span class="cart--button-text">{{ 'products.product.add_to_cart' | t }}</span>
              {%- endif -%}
            </button>
          </div>
          <div class="d-sm-none">
            {{ form | payment_button }}
          </div>
        </div>
      {%- endform -%}
    </product-form>
  </div>
</div>
